---
layout: default
---
<div id="content">

<h1>{{ page.judul }}</h1>

<div id="tags">
{% assign authorCount = page.tags | size %}
{% if authorCount == 0 %}
    No author
{% elsif authorCount == 1 %}
{% assign tag = site.tags | where: 'short_name', page.tag | first %}
    <a class="tag" href="{{ tag1.url }}">{{ tag.name }}</a>
{% else %}
    {% for tag in site.tags | where: 'short_name', page.tag | split: ", " %}
        {% if forloop.first %}
               <a class="tag" href="{{ tag.url }}">{{ tag.name }}</a>
        {% else %}
                <a class="tag" href="{{ tag.url }}">{{ tag.name }}</a>
        {% endif %}
    {% endfor %}
{% endif %}
</div>

<div id="post">
{{ content }}
</div>
</div>
